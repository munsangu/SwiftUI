import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Main View")
                .font(.largeTitle)
                .foregroundStyle(.white)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
