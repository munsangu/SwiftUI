import SwiftUI

struct TabScreenView: View {
    let tab: Tab
    
    var body: some View {
        NavigationStack {
            VStack {
         
            }
            .navigationTitle(tab.title)
        }
    }
}
